# Shell Prompts

This directory documents shell prompt architecture standards.

The live prompt implementation may live in `.dotfiles`, but this directory defines the public standard and vocabulary for how prompts should be designed.

Known prompt styles include:

```text
operator
system_default
```

Additional prompt styles can be added here as they become formalized.
