# Sanitization

Document what is safe to publish and what must remain private.

Use `private/` for raw local material.

Use `training/` for synthetic or sanitized samples.

Before public release, review:

- docs
- templates
- config snapshots
- screenshots
- captures
- git history risk
